export interface Theme {
  colors: {
    text: string;
    card: string;
    primary: string;
    background: string;
  };
}

export const theme: Theme = {
  colors: {
    text: '#000000',
    card: '#FFFFFF',
    primary: '#007AFF',
    background: '#F2F2F2',
  },
};
